const CREDENTIALS_PATH = "./chemin/vers/credentials.json"; // Adapter le chemin selon votre structure

module.exports = {
  CREDENTIALS_PATH: CREDENTIALS_PATH,
  // Autres configurations si nécessaire
};
